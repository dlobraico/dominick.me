This is the OCaml application that runs my personal website. 

The most reliable place to determine the current dependences is to look at the
_tags file in this directory.

Compile with `ocamlbuild -use-ocamlfind src/dal.native` in this directory.
